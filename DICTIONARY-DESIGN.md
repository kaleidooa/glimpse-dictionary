# Glimpse 0.3 · 빠른 영한 사전

2026-09-12. 기본 경로는 **단어 탐지 → 정규화·활용형 처리 → 기기 영한 DB → 품사별 한국어 뜻**이다. 외부 사전이나 LLM을 켜지 않아도 동작한다.

## 문제와 결정

0.2는 한국어 뜻이 121개뿐이고, 나머지는 선택형 Free Dictionary API에 의존했다. 현재 개발 환경에서 `serendipity`, `resilient`, `abandon`, `went`, `cohesive`를 각각 8초 제한으로 요청했으나 모두 시간 초과였다. PowerShell의 별도 요청도 시간 초과였다. 이 결과만으로 서비스 전체의 장애나 사용자의 Chrome에서 발생한 정확한 원인을 단정할 수는 없다.

0.3은 사전을 패키지에 포함해 외부 API 상태와 어휘 범위를 분리했다. 보조 영영 API는 사용자가 켜고 사이트 접근을 허용한 경우에만 기기 DB의 누락 단어를 조회한다. 2.5초 제한과 미등록·연결 실패·시간 초과·요청 제한·서버 오류를 구분했다. 확장 팝업의 ‘온라인 연결 확인’은 동의한 경우 고정 테스트 단어 하나로 실제 연결을 확인한다.

## 데이터와 품질

| 데이터 | 포함 범위 | 역할 / 배포 조건 |
|---|---:|---|
| 한국어 위키낱말사전, Kaikki 추출 | 13,649개 표제어 / 19,747개 뜻 | 기본 공개 사전. CC BY-SA 4.0 |
| Kengdic | 45,771개 영어 표제어 / 73,535개 대응 뜻 | 위키에 없는 단어의 보조 사전. MPL 2.0 선택 |
| 두 공개 DB의 합집합 | **51,109개 표제어 / 6,109,073바이트** | 겹치는 단어는 한 번만 셈. 뜻 데이터만 약 6.1MB |
| WordNet 3.0 예외 목록 | 5,635개 활용형 철자 | 불규칙 동사·명사·비교급 원형 후보. Princeton 이용 조건 포함 |
| Glimpse 기본 뜻 | 기존 121개 + 추가 교정 표제어 50개 | 흔한 독해·기술 용어에 대해 별도로 작성. 공개 사전과 분리 |

원본은 [한국어 위키낱말사전의 Kaikki 영어 데이터](https://kaikki.org/kowiktionary/%EC%98%81%EC%96%B4/index.html), [Kengdic upstream](https://github.com/garfieldnate/kengdic), [WordNet](https://wordnet.princeton.edu/license-and-commercial-use)을 사용했다. 위키의 출처 조건은 [한국어 위키낱말사전 저작권 안내](https://ko.wiktionary.org/wiki/위키낱말사전:저작권)에 따른다. Kengdic은 upstream에 명시된 이중 라이선스 중 MPL 2.0을 선택했다.

한 단어짜리 영어 표제어, 한국어 뜻이 있는 항목만 가져온다. 공백·일부 마크업을 정리하고 중복을 제거한다. Kengdic의 영어 설명은 **전체가 단어 하나인 경우에만 역색인**하며 쉼표로 무작정 쪼개 뜻을 붙이지 않는다. 원본 해시, 건수와 변환 내역은 `public/dictionary/manifest.json`, 변환 코드는 `scripts/import-dictionary.mjs`에 있다. 두 데이터베이스는 서로 다른 파일과 라이선스로 배포한다. 가공한 JSON, 변환 코드, 저작자·라이선스 안내를 확장에도 포함한다.

공개 데이터는 완성된 상용 영한 사전과 같지 않다. Kengdic upstream도 데이터 품질 문제를 설명한다. 실제 검사에서 `initiative`의 대표 뜻이 부자연스러웠고 위키의 `evidence`는 흔한 명사 뜻이 누락되어 있었다. 이처럼 발견한 항목과 기초 누락어는 Glimpse 기본 뜻으로 교정했다. **전체 5만 단어의 의미를 전문가가 검수한 것은 아니다.** 전문 용어, 최신 용어, 오탈자와 문맥에 맞는 뜻 선택은 추가 검증이 필요하다.

다른 집계 DB인 `open-english-korean-dict`도 살펴봤으나 원본 Kengdic 라이선스와 집계 설명이 일치하지 않고 일부 대표 뜻이 어색해 사용하지 않았다. 이번 공개 DB에는 그 집계의 생성 번역을 가져오지 않았다.

## 조회와 표시

알파벳별 JSON 파일을 필요한 때 읽으며 최대 12개 파일을 메모리에 유지한다. 같은 파일의 동시 요청은 공유한다. 파일 로딩 실패는 ‘사전 파일 확인 필요’로 표시하고 다음 요청에서 다시 읽는다. 사용자의 조회 이력이나 문장을 저장하는 캐시는 아니다.

WordNet 예외와 규칙을 함께 사용한다. 규칙으로 만든 원형은 다시 활용했을 때 입력 철자가 나오는지 확인한다. `hoped/hoping → hope`, `hopped/hopping → hop`, `went → go`, `children → child`를 구분한다. `news`, `business`, `scissors` 같은 등록 명사는 보존한다. `saw`와 `making`처럼 자체 표제어와 활용형이 겹치면 가능한 해석을 함께 제시한다.

커서 팝업은 처음 3개 뜻을 품사와 함께 표시하고 ‘다른 뜻 더 보기’로 최대 8개를 펼친다. 원형, 공개 사전 이름, 라이선스와 관련 표제어 출처 링크도 표시한다. 특정 문장을 해석했다고 주장하지 않는다. 스크롤 시 닫기, 180ms 안정화, 늦은 응답 무시, 입력창 제외는 유지한다.

## DB / 가벼운 모델 비교

| 선택지 | 이 용도에서의 장점 | 제약 / 이번 결정 |
|---|---|---|
| 기기 DB + 코드 | 짧은 응답 시간, 요청 요금·키 불필요, 외부 전송 없이 출처 있는 뜻 표시 | 누락과 다의어는 남는다. **기본 경로로 구현** |
| 작은 서버 LLM | 문맥에 맞는 뜻 선택, 신조어 설명 가능 | 네트워크 지연, 키를 보관할 서버, 요금, 문장 전송과 오답 가능성. 확장 기본 경로에 넣지 않음 |
| Chrome Translator API | 기기에서 영어·한국어 번역 가능 | 모델 최초 다운로드·지원 환경·사용자 동작이 필요하고 Web Worker에서 사용할 수 없어 현재 확장 service worker에 바로 연결할 수 없음. 후속 선택 기능 후보 |
| 브라우저의 범용 로컬 LLM | 문맥 처리를 기기 안에서 수행할 수 있음 | 모델 다운로드와 기기 요구 조건이 소형 사전 DB보다 큼. 기본 설치에는 포함하지 않음 |

모델 선택은 문서와 구조를 비교한 판단이며, **이번 작업에서 LLM의 실제 지연·정답률·비용을 실험하지 않았다.** 모델을 추가한다면 먼저 사전에서 후보 뜻을 가져오고 사용자가 동의한 문장에 맞는 후보를 고르는 보조 기능으로 평가하는 것이 적합하다. 기존 localhost 시선 실험실의 서버 키 선택 기능은 유지하되 DB에 없는 단어에만 시도한다. Chrome 확장은 OpenAI로 문장을 보내지 않는다.

비교 근거: [Chrome Translator API](https://developer.chrome.com/docs/ai/translator-api), [Chrome 기기 AI 요구 조건](https://developer.chrome.com/docs/ai/get-started), [OpenAI의 작은 모델 문서](https://developers.openai.com/api/docs/models/gpt-5-nano).

## 실제 검증과 한계

- 공개 DB **51,109개 표제어 전부 조회 가능** 확인. 이것은 데이터 연결 검사이며 뜻의 정확도 100%를 의미하지 않는다.
- 실제 데이터의 대표 뜻, 활용형, 다의어, 출처, 캐시, 외부 전송 동의, 오류 구분을 자동 검사한다. 기존 커서·시선 실험 회귀도 유지한다.
- 작성자가 고른 독해·기술·활용형 목록 398개 중 첫 검사에서 382개가 조회되었다. 누락된 흔한 단어 16개를 보강한 뒤 398개 모두 조회되었다. **같은 목록을 보고 보강했으므로 독립 평가나 일반적인 100% 정확도 수치로 사용할 수 없다.** 보강 전/후 결과를 `reports/`에 함께 보관했다.
- 250회 새 캐시, 2,000회 서로 다른 단어, 1,000회 같은 단어 재조회 속도를 측정했다. 마지막 실행의 P95는 각각 약 6.7ms, 7.7ms, 0.015ms였다. Node에서 실제 파일을 읽은 값이며 Chrome IPC·화면 렌더·프로세스 시작과 OS 콜드 캐시는 제외한다.
- 실제 Chrome의 localhost 화면에서 보조 사전을 끈 채 `serendipity`, `went`, `children`, `bank`, `hoped`, `hopped` 조회를 확인했다. 화면의 측정값은 2~60ms였으며 외부 웹사이트 확장 전체의 성능 보증은 아니다. 미등록 단어도 명시적으로 안내했다.
- 새 패키지의 service worker는 Chrome API 대역과 실제 패키지 파일로 실행 검증한다. **실제 Chrome에서 0.3 확장을 다시 로드한 뒤의 service worker/권한/단축키 통합 확인은 별도**다. 0.2 테스트 페이지의 커서 동작은 사용자가 정상이라고 확인했다.

재현: `npm test`, `node scripts/benchmark-dictionary.mjs`, `npm run build`, `npm run build:extension`, `node scripts/check-extension.mjs`.

다음 정확도 평가는 수정에 쓰지 않은 실제 기사·기술 문서의 단어와 문장을 따로 고정하고, 사람의 정답 판정으로 ‘원형 정확성 / 유용한 뜻 포함 여부 / 첫 뜻 적합성 / 미등록률’을 구분해야 한다. 새 로그를 자동 수집하지 않으며, 사용자가 공유한 실패 단어를 회귀 사례로 추가할 수 있다.
