# Glimpse 0.4 검증 기록

2026-09-12, Windows / Node 24.19.0. 공개 베타를 위한 검사이며 Chrome Web Store 심사를 통과했다는 의미가 아닙니다.

## 자동 검사

- `npm test`: **16개 파일, 152개 테스트 통과**. 커서·스크롤·시선 실험 회귀와 공개 DB, 활용형, 외부 API 실패 처리 검사를 유지했습니다.
- 단어장은 사용자가 저장하기 전 기록하지 않는지, 동시 저장과 중복 방지, 학습 상태, 삭제, JSON 왕복·병합 복원, 잘못된 파일의 원자적 거부, 저장 오류, CSV 수식 방지와 출처 보존을 검사했습니다.
- 웹페이지의 content script는 단어 저장만 요청할 수 있고, 목록 열람·삭제·학습 상태 변경·가져오기는 확장 소유 화면에서만 허용되는지 검사했습니다. 일반 페이지로 단어장 전체를 전달하지 않습니다.
- 처음 설치 때만 안내 화면을 열고 업데이트 때는 열지 않는 동작, 단어장 변경이 사이트 스크립트 재등록을 유발하지 않는 동작도 검사했습니다.
- 실제 UI 컴포넌트에서 처음 조회 → 직접 저장 → 단어장 → 학습 상태·필터 → 삭제를 확인했습니다. 일반 웹 데모에서 Chrome 권한 설정을 조작할 수 없도록 검사했습니다.
- `npm run build`, `npm run build:extension`: TypeScript와 production 빌드 통과.
- `npm run check:extension`: **74개 필수 파일과 19개 스크립트·스타일 참조** 확인. 실제 빌드된 service worker를 Chrome API 대역으로 실행해 한국어 단어 9개를 로컬 파일에서 조회했습니다. 외부 요청은 차단했습니다. 같은 번들에서 저장·중복 방지·학습 상태·삭제·메시지 권한 경계를 확인했습니다.
- ZIP은 manifest 버전과 52개 사전 shard를 검사하고 SHA-256 파일을 함께 만듭니다. 소스에는 개인 설정·실험 데이터·생성된 패키지를 포함하지 않으며, `npm run check:public`은 제외 경로와 알려진 인증정보 패턴을 검사합니다. 이 검사가 모든 비밀 탐지를 보증하지는 않습니다.
- 공개 DB의 표제어 **51,109개**, 한국어 뜻 **93,282개**에 대한 파일·전수 조회 검사는 0.3에서 수행했고 데이터는 그대로 유지했습니다. 작성자 선정 398개 목록의 보강 전후 결과와 3,250회 속도 측정은 `reports/dictionary-benchmark*.json`에 있습니다. 보강에 사용한 목록이므로 독립 정확도 평가가 아닙니다.

## 실제 Chrome에서 확인한 범위

localhost의 0.4 처음 사용 화면에서 `serendipity`를 조회하고 직접 저장했습니다. 단어장에서 한국어 뜻 검색, 학습 상태 변경, 필터, **새로고침 후 저장·학습 상태 유지**, 개별 삭제를 확인했습니다. 기존 단어장이 비어 있음을 확인한 후 만든 테스트 항목만 삭제했습니다. 사용자 시선 실험 기록은 변경하지 않았습니다.

좁은 Chrome 화면에서 안내·단어장 레이아웃을 확인했습니다. 이 확인은 개발용 웹 화면이며 설치된 확장의 IPC·권한 동작을 포함하지 않습니다.

CSV 다운로드 이벤트는 자동화 도구에서 시간 초과되었고, JSON 파일 선택은 도구 권한 제한으로 거부되었습니다. **파일 다운로드·선택·복원 전체를 실제 Chrome에서 완료했다고 주장하지 않습니다.** 생성되는 CSV·JSON 내용과 복원 로직은 자동 테스트로 검증했습니다.

0.3에서 실제 Chrome UI로 확인한 `went → go`, `children → child`, `bank`, `hoped → hope`, `hopped → hop`, 미등록 단어 처리도 자동 회귀 범위에 유지합니다.

## 아직 확인하지 못한 범위

Chrome 내부 확장 관리 탭을 자동 제어할 수 없어 **설치된 0.4 확장을 다시 로드한 뒤 단축키·service worker·권한·단어장을 연결한 전체 흐름은 수동 확인이 남아 있습니다**. 브라우저 프로필 파일을 수정하거나 이를 우회하지 않았습니다.

다양한 사이트·확대율·운영체제, 실제 문맥에 맞는 첫 뜻, 데이터 전체의 의미 품질, 웹캠 정확도, 유료 LLM 실호출은 별도 평가가 필요합니다. 자동 테스트 수와 조회 건수는 실제 사용자 정확도가 아닙니다.

## 설치 후 확인 순서

1. `chrome://extensions` → 기존 Glimpse의 ↻ → 읽던 페이지 F5. 확장 팝업의 **v0.4.0**을 확인합니다.
2. 팝업에서 `serendipity`와 `went`를 조회합니다. 기본 설정에서 기기 영한 사전만으로 결과가 나와야 합니다.
3. 일반 영어 웹페이지에서 ‘이번 탭에서 사용’ → 단어 위에 커서 → Alt+Shift+D. 페이지·내부 영역을 스크롤한 뒤에도 새 단어를 조회합니다.
4. 다른 뜻·출처·Esc·스크롤 닫기를 확인합니다. ‘단어장에 저장’ → 단어장 → 검색·익숙해요 → 새로고침으로 유지 여부를 확인합니다.
5. JSON 백업 다운로드 → 해당 테스트 단어만 삭제 → JSON 가져오기로 복원합니다. CSV의 한글·출처도 확인합니다.
6. 사이트 자동 사용 권한을 켰다가 해제하고, 해당 사이트의 자동 실행이 멈추는지 확인합니다. 저장 단어는 남아 있어야 합니다.
7. 외부 영영 사전이 필요한 경우에만 설정을 켜고 ‘온라인 연결 확인’을 누릅니다. 외부 연결 실패에도 기기 사전은 사용할 수 있어야 합니다.
8. 시선 베타는 별도 실험실에서 새 평가로 판단합니다. 실제 독해 단어와 문맥을 별도로 선정해 유용한 뜻·첫 뜻·누락률을 평가합니다.