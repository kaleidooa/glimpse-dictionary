# Glimpse 0.4 설치와 사용

## 처음 설치

1. GitHub Releases의 `Glimpse-0.4.0.zip`을 다운로드하고 폴더에 압축을 풉니다.
2. Chrome에서 `chrome://extensions`를 엽니다.
3. 개발자 모드를 켜고 ‘압축해제된 확장 프로그램 로드’에서 `manifest.json`이 있는 폴더를 선택합니다.
4. 처음 사용 안내의 직접 조회를 시험하거나, 일반 영어 웹페이지로 이동합니다.
5. 확장 아이콘에서 **이번 탭에서 사용**을 누릅니다. 창을 닫고 단어 위로 커서를 옮긴 뒤 **Alt + Shift + D**를 누릅니다.

개발자는 `npm ci` → `npm run build:extension` 후 생성된 `dist-extension` 폴더를 로드합니다. 설치 후 개발 서버는 필요하지 않습니다. 확장 폴더는 그대로 보관해야 합니다.

## 업데이트

기존 폴더의 파일을 새 버전으로 교체한 뒤 **확장 관리 화면의 Glimpse ↻ → 읽던 페이지 F5** 순서로 새로고침합니다. 기존 단어장을 유지하려면 확장을 삭제해서 다시 설치하지 말고 같은 확장을 갱신하세요. 만일을 위해 단어장의 JSON 백업을 먼저 내려받을 수 있습니다.

이 프로젝트의 `dist-extension`을 이미 설치했다면 빌드 후 그 폴더가 업데이트됩니다. 팝업 아래의 `v0.4.0` 표시를 확인하세요.

## 단어 뜻과 단어장

- 기기 사전은 항상 켜져 있습니다. `serendipity`, `went`, `children`, `bank`를 보조 영영 사전 없이 조회할 수 있습니다.
- 팝업에 품사와 대표 뜻 3개를 표시하고 다른 뜻을 펼칠 수 있습니다. 원형과 사전 출처 링크도 확인할 수 있습니다.
- **단어장에 저장**을 직접 누른 단어만 보관합니다. 일반 조회 이력·원문 문장·방문 주소는 저장하지 않습니다.
- 확장 아이콘 → **단어장**에서 검색, 익숙한 단어 표시, 개별 삭제, CSV·JSON 내려받기와 JSON 복원이 가능합니다. 가져오기는 기존 단어를 유지하며 빠진 항목만 추가합니다.
- 최대 1,000개 또는 약 4MB까지 저장합니다. 계정과 자동 동기화는 사용하지 않습니다. 확장 삭제 시 단어장이 사라질 수 있으므로 JSON 백업을 보관하세요.

## 사이트와 보조 사전 설정

팝업의 **설정**을 누르면 단축키, 보조 사전, 전체/개별 사이트 권한, 시선 실험실을 한 화면에서 관리합니다.

‘이 사이트에서 항상 사용’을 선택하면 해당 호스트에 자동 적용합니다. 다른 출처의 iframe은 별도 사이트 권한이 필요할 수 있습니다. ‘모든 웹사이트에서 자동 사용’은 선택형 전체 HTTP/HTTPS 권한을 요청합니다. 이 설정을 끄면 전체 권한과 개별 사이트 목록, 보조 영영 사전 설정도 해제합니다. 단어장은 유지합니다.

보조 영영 사전은 DB에 없는 단어 하나만 dictionaryapi.dev로 보내 영어 뜻을 가져옵니다. 문장·페이지 주소·쿠키를 보내지 않으며 외부 서버에는 IP가 보일 수 있습니다. 연결 실패·시간 초과·미등록·요청 제한을 구분해 안내합니다. 설정의 **온라인 연결 확인**으로 따로 검사할 수 있습니다.

## 단축키가 작동하지 않을 때

1. 일반 HTTP/HTTPS 웹페이지인지 확인합니다. Chrome 내부 페이지·웹 스토어·PDF 뷰어·이미지 속 글자는 지원하지 않습니다.
2. 확장 팝업에서 이번 탭 또는 이 사이트 사용을 켭니다.
3. 팝업을 닫고 단어 위로 커서를 조금 움직인 뒤 단축키를 누릅니다.
4. 입력창에 글을 쓰는 중이거나 스크롤 중이라면 조회하지 않습니다. 스크롤이 멈춘 뒤 다시 누르세요.
5. `chrome://extensions/shortcuts`에서 단축키가 등록되었는지 확인합니다.
6. 확장을 업데이트했다면 읽던 페이지를 새로고침합니다.

Esc, 바깥 클릭, 스크롤과 창 크기 변경은 뜻 팝업을 닫습니다. 웹페이지 텍스트를 감싸거나 원래 선택 상태를 바꾸지 않습니다.

## 시선 실험실

설정 → **시선 추적 실험실 BETA**에서 별도 탭으로 엽니다. 카메라는 직접 연결해야 시작합니다. 일반 웹사이트에서는 시선을 수집하지 않습니다. 영상은 저장·전송하지 않지만 실험 특징·문장·시선 기록은 해당 실험실 저장소에 보관합니다. 기존 localhost 실험 기록과 확장 기록은 분리되어 있습니다.

자세한 내용: [개인정보](PRIVACY.md) · [사전 설계](DICTIONARY-DESIGN.md) · [검증](EXTENSION-VALIDATION.md) · [시선 실험실](docs/EYE-LAB.md).
